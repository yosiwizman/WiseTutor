# Phase 5 Slice 3B — Real Piper Backend Runtime — Result 2026-04-13

## Summary

Piper TTS binary and voice model installed; backend `/api/v1/voice/synthesize`
confirmed to return a real WAV file with `x-wt-tts-engine: piper`. Audible
output through speakers has NOT been confirmed by a human in this session.

---

## Install

- Method: `pip install piper-tts` inside the repo virtualenv.
- Version: `piper-tts 1.4.2`
- Binary path: `/home/ai-desktop/projects/WiseTutor/.venv/bin/piper`

## Voice model

- Source: `rhasspy/piper-voices` HuggingFace mirror
- Model: `en_US-lessac-low` (low quality, smallest viable, no GPU required)
- ONNX file: `/mnt/models/piper/en_US-lessac-low.onnx` — 63 201 294 bytes
- Config file: `/mnt/models/piper/en_US-lessac-low.onnx.json` — 4 882 bytes

## Backend env (restart at ~13:12Z 2026-04-13)

```
WISETUTOR_PIPER_BIN=.venv/bin/piper
WISETUTOR_PIPER_VOICE_PATH=/mnt/models/piper/en_US-lessac-low.onnx
```

## TTS-status endpoint

Request: `GET /api/v1/voice/tts-status`

Response:
```json
{"test_mode": false, "bin": ".../piper", "voice_path_set": true, "max_chars": 5000}
```

## Synthesize endpoint

Request: `POST /api/v1/voice/synthesize`
Body: `{"text": "hello world from the real piper backend"}`

Response headers (selected):
```
HTTP/1.1 200 OK
x-wt-tts-engine: piper
content-type: audio/wav
content-length: 74284
```

Response body: 74 284 bytes saved as `synth_hello_world.wav` in this directory.

WAV file identification (via `file` command):
```
synth_hello_world.wav: RIFF (little-endian) data, WAVE audio,
  Microsoft PCM, 16 bit, mono 16000 Hz
```

## Pytest regression

`tests/api/test_voice_router.py` — 12 passed in 0.38s (6 STT + 6 TTS). No failures.

## Known gap

Real audible output through speakers has NOT been confirmed by a human in this
session. The WAV file is present and structurally valid (RIFF/PCM/16-bit/mono
16000 Hz), but subjective listening proof (Tier 1 audible claim) requires a
human to play the file and confirm it sounds correct. This remains Tier 3
until that confirmation occurs.

## Recovery note

Agent A's earlier voice.py backend additions (commit 116b804) did NOT persist
to disk — the edits were lost before the commit landed. The backend code was
re-applied in this task.
