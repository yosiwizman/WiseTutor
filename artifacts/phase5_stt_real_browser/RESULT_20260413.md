# Phase 5 Slice 1 — Real Chrome native-mic proof

## Session

- Date / time (UTC): prep 2026-04-13T14:13:49Z; founder Chrome dictation window captured 2026-04-13 ~14:23Z (file mtimes on the screenshots: 10:23–10:25 local = ~14:23–14:25 UTC)
- Machine: ai-desktop-System-Product-Name (Ubuntu 24.04, display :1)
- Browser: Google Chrome 146.0.7680.177
- Frontend URL: http://localhost:3782 (Chrome URL bar visible in screenshot)
- Backend URL: http://localhost:8001 (running on same host, voice router live)
- Microphone hardware available: ALC1220 Analog (card 2) and USB PnP Sound Device (card 4) — screenshots do not disambiguate; Chrome used whichever was the OS default input

## Test A — Mr W real Chrome dictation

1. Signed in: **yes** — screenshot shows the signed-in workspace ("DeepTutor" sidebar, Mr W profile avatar bottom-right of composer row).
2. Composer empty at start: **yes** — `chrome_real_mic_01_before.png` shows empty composer with placeholder "How can I help you today?".
3. Mic permission: **yes** (transcript appeared in composer, which is only possible after Chrome granted mic permission in a real Web Speech session).
4. Spoken phrase: **"audit one do not send"**.
5. Final composer text visible in `chrome_real_mic_03_no_auto_send_evidence.png`: `audit one do not send` (matches the dictated phrase exactly, lowercase — Web Speech / Chrome behavior).
6. Auto-send: **no** — the chat thread shows the default empty-state hero ("What would you like to learn? / Ask anything — I'm here to help you understand."). No user-message bubble rendered, no assistant reply. The draft is still in the composer textarea, unsubmitted.
7. Caveats:
   - No separate `_02_transcript_in_composer.png` was captured — the founder uploaded two distinct images; file `_03` single-handedly demonstrates both (a) the dictated transcript in the composer and (b) no auto-send (empty chat thread). This is a naming/packaging loss, not an evidence loss.
   - The `chrome` file originally saved without extension was a byte-identical duplicate of the pre-state; it was renamed to `_01_before.png` and the duplicate at `_02` slot was removed.

## Screenshots (saved under `artifacts/phase5_stt_real_browser/`)

- [x] `chrome_real_mic_01_before.png` — composer empty, mic idle, DevTools Network panel open, workspace loaded at http://localhost:3782
- [ ] `chrome_real_mic_02_transcript_in_composer.png` — **not captured as a separate file**; the content expected here is visible inside `_03` (the composer textarea contains "audit one do not send")
- [x] `chrome_real_mic_03_no_auto_send_evidence.png` — composer draft reads "audit one do not send", chat thread still shows the empty workspace hero (no user bubble, no assistant reply) → proves transcript presence AND no auto-send

## Result

- **PASS.**
- Claim granted: **Tier 1** for the browser-native Chrome STT path on this machine. Proven by independent re-execution against real-world conditions — real Chrome 146, real microphone permission granted, dictated phrase "audit one do not send" landed in the composer, no message auto-sent, artifacts saved on disk.
- Claim NOT granted (out of scope): cross-machine reproducibility, Firefox fallback, real faster-whisper on real audio (Phase B still pending).

## Sign-off

- Founder: Yosi (provided the Chrome session + screenshots)
- Agent: Claude Code (Opus 4.6) — performed artifact salvage (rename/dedupe) and completion of this RESULT file
